# CPP05 ex03 設計比喩 — Intern（窓口）と Bureaucrat（官僚）

`解説` の補助資料。ex03 の役割分担を、組織の比喩で腹落ちさせるためのメモである。

## 比喩の骨格

- **Intern（インターン）** は大臣（`main`）から指示された書類名をテーブルで引き、対応する書類を生成して大臣に返す「総務の窓口」が仕事で、審査・署名・実行の権限は持たない。
- **Bureaucrat（官僚）** は、渡された書類を等級で審査し、「署名・実行」を行うのが仕事である。

この分け方は、ex03 のクラス責務と一致する。

---

## CPP05 世界観のメタファー（議会と行政）

### 1. `main` / ユーザー ＝ 大臣（行政のトップ）

* **仕事：** 「この書類を用意してくれ」「署名して実行しろ」と指示を出す。
* **特徴：** 木の植え方やロボトミーの機械の動かし方といった現場の詳細は知らない（知る必要がない）。`AForm*` という抽象的な決裁書類を受け取り、官僚にハンコを押させる。

### 2. `Intern`（生成役） ＝ 受付窓口

* **仕事：** 大臣から「『ロボトミー要求』の書類を作って」と文字列で頼まれる。手元の「書類名と作成係の対応表」を確認し、該当する具体型を生成して大臣に差し出す。
* **特徴：** 各具体型フォームのヘッダ（専門知識）を知っているのはこの部署だけ。書類の種類を増やすとき、書き足す場所が窓口（`Intern`）にまとまる（Open-Closed）。

### 3. `AForm` / 具体型フォーム ＝ 各省庁の申請書類

* **仕事：** 共通フォーマット（名前、署名等級、実行等級）を持ちつつ、「木を植える」「ロボトミーを実行する」など固有の処理を持つ。

### 4. `Bureaucrat` ＝ ベテラン官僚

* **仕事：** 等級（Grade）をチェックして「署名（`signForm`）」し、条件を満たせば「実行（`executeForm`）」する。
* **特徴：** 書類を作る（`new` する）権限は持たない。作成と審査を分けるのが、`Intern` との関心の分離である。

---

## このストーリーで学ぶ設計上のポイント

1. **結合度を下げる**  
   大臣（`main`）が全具体型のヘッダを include して `new` すると、書類を足すたびに大臣側も直すことになる。窓口（`Intern`）を挟むと、大臣は具体型を知らなくてよい。

2. **所有権の所在**  
   窓口がヒープ上に作って渡した書類を、最終的に破棄（`delete`）するのは、指示した大臣（`main`）の責任である。

同じ形の問題は、チーム開発でも層の境界に出る。IRC では次のように対応する（IRC 未経験でも、右列は「境界の向こう側の内部を知らない／選ぶ表を一箇所にまとめる」と読めば足りる）。

| 考察の比喩 | 設計の中身 | IRC側の対応 |
|---|---|---|
| 大臣は具体書類を知らない | 呼び出し側は `AForm*` だけ見る | A層は B の内部を知らず、契約（`CommandResult` 等）だけ見る |
| 窓口（Intern）に対応表をまとめる | 生成規則の集約（実質の SSOT） | B層の dispatcher：コマンド名 → ハンドラ |
| 官僚は `new` しない | 関心の分離 | B は `send()` せず結果を返す／C の内部を直接触らない |
| 大臣が `delete` | 所有権の所在 | 誰が生成し、誰が寿命を持つか（層境界の責任） |

「誰が何を知っていて、誰が何をする責任があるか」を組織の役割に置き換えておくと、ポインタの所有権やポリモーフィズムも追いやすい。実装手順そのものは [解説](CPP05_ex03_解説.md) を正とする。
