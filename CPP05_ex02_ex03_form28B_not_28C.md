# ex02の「28Bじゃなくて28C」ドタバタ → ex03 Intern で窓口化

Exercise 02 の副題 **「No, you need form 28B, not 28C...」** は、官僚主義のたらい回しと、**手作業で書類の種類を取り違えるミス**を風刺したフレーズである。

このメモでは、次の順で対比する。

1. **ex02（インターンなし）**: 呼び出し側が具体クラスを `new` し、28B / 28C を取り違えるドタバタ
2. **ex03（Intern あり）**: 書類名の文字列を窓口に渡し、生成を一箇所にまとめる

ヴォゴン人の無限スタンプラリー（『銀河ヒッチハイク・ガイド』）は雰囲気の比喩に留め、コードの焦点は「誰がどの書類を知っているか」に置く。

---

## 1. ex02 — インターンがいない窓口のドタバタ

ex02 までは、フォームが必要になるたび、呼び出し側（`main` など）が具体クラス名を書いて `new` する。

呼び出し側は「28B と 28C の違い」「コンストラクタ引数」「ヘッダの include」を全部知っていなければならない。知らないと、次のような取り違えが起きる。

```cpp
// ex02: 使う側が具体型と引数を正確に知っている必要がある
// 「ロボットミー要求」のつもりで、別の書類（28C 相当）を作ってしまう例
AForm* form = new PresidentialPardonForm("Bender"); // 欲しかったのは RobotomyRequestForm

bureaucrat.signForm(*form);
bureaucrat.executeForm(*form);
delete form;
```

ドタバタの中身は次である。

- 欲しい書類の**意味**（「ロボトミー要求」）と、選ぶ**型名**が呼び出し側の頭の中でだけ対応している
- 型を間違えてもコンパイルは通ることが多く、実行して初めて「違う書類だった」と分かる
- 書類の種類を足すたびに、呼び出し側の `new` と `#include` が増える

「必要なのは 28C じゃなくて 28B だ」と言い直される地獄は、コード上では **呼び出し側が具体型を直書きしている** ところに現れる。

---

## 2. ex03 — Intern で一定の解決

ex03 の `Intern` は、書類名の文字列から具体型を生成する窓口である。呼び出し側は `AForm*` と `Intern` だけを知ればよい。

```cpp
// ex03: 文字列を渡す。どの具体型を new するかは Intern 側にまとまる
Intern intern;
AForm* form = intern.makeForm("robotomy request", "Bender");
if (form == NULL) {
    // 存在しない書類名 → 窓口が拒否（無効なオブジェクトを作らない）
    return;
}
bureaucrat.signForm(*form);
bureaucrat.executeForm(*form);
delete form; // 所有権は指示した側（main）が持つ
```

対比の要点だけ書く。

| | ex02（下手な呼び出し側） | ex03（Intern） |
|---|---|---|
| 誰が具体型を知るか | `main` が全部知る | `Intern` にまとまる |
| 取り違えの形 | 型名を直書きしてミス | 文字列ミスは `NULL` / エラーで返せる |
| 種類を足すとき | 呼び出し側の `new` / include が増える | 書き足す場所が `Intern` になる |

長いシナリオ（権限不足・存在しないフォーム・詩の苦行…）は、ここには載せない。署名・実行・例外の流れは [ex02 解説](CPP05_ex02_解説.md) / [ex03 解説](CPP05_ex03_解説.md) 側を見る。

---

## 3. 設計として何が改善されているか（いったん列挙）

後で解説・設計比喩と重複したら削ってよい。ここでは対比の「なぜ楽になるか」だけ残す。

### 3.1 手作業の `new` を窓口にまとめる

- ex02: 呼び出し側が `new RobotomyRequestForm(...)` などを直書きする
- ex03: `makeForm("robotomy request", target)` を呼び、**生成規則を Intern 一箇所に書く**（Factory に近い形）

### 3.2 呼び出し側の分岐と取り違えを減らす

「28B ではなく 28C」という手作業ミスや、呼び出し側に散らばる `if (type == ...) new ...` を避ける。  
課題が拒否する過剰な if 連鎖の話は [ex03 解説](CPP05_ex03_解説.md) を正とする。

### 3.3 結合度（DIP / OCP の言い方）

- **DIP**: 呼び出し側は `ShrubberyCreationForm` 等の具体クラスに直接依存せず、`AForm` と `Intern` を見る
- **OCP**: 新しい書類（「フォーム 28D」）を足すとき、まず直すのは `Intern` の対応表であり、大臣役の `main` を毎回書き換えなくてよい

役割の比喩（大臣・窓口・官僚）は [設計比喩](CPP05_ex03_設計比喩.md) を参照。

---

## まとめ

副題が示す「書類の種類を取り違えて作り直される地獄」は、ex02 では **呼び出し側が具体型を `new` している** ことに対応する。  
ex03 の `Intern` は、その生成を窓口にまとめ、呼び出し側の密結合と取り違えを減らす。官僚主義そのものが消えるわけではないが、「どの書類を机に乗せるか」で迷う部分は一定の解決になる。
